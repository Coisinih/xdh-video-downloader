# VidNest 项目总结

## 1. 项目定位

VidNest 是一个前后端分离的自托管公开视频下载工具。用户粘贴公开视频链接，系统解析标题、封面、时长与可用格式后，由浏览器触发下载。

项目封装 `yt-dlp` 源码，不修改其上游实现；`ffmpeg` 用于服务端下载时的媒体合并。

## 2. 已交付功能

- Vue 3 + Vite 简体中文下载页，适配桌面与移动端。
- FastAPI 解析接口：返回视频元数据、封面与格式列表。
- Bilibili 视频解析会使用公开详情接口补全作者、简介和播放量，避免提取器仅返回格式时在页面显示“未知”。
- 同源封面代理，避免 HTTP 封面、跨域或 Referer 导致浏览器不显示封面。
- 浏览器下载：直连可 302 到媒体源；需要 Referer、User-Agent 或 Origin 时自动使用流式代理，并支持 Range 请求。
- 服务端下载任务：进度、临时文件与两小时有效的交付地址。
- 内存化任务、解析与交付令牌；服务重启后清空。
- SSRF 防护、格式 ID 白名单、受控子进程调用、超时/并发/大小限制及临时记录清理。
- Docker Compose 部署：Nginx 前端反代与 FastAPI 后端。

## 3. 已验证平台与示例

以下公开样本均已使用当前 vendored `yt-dlp` 解析；AcFun 和虎牙完成了完整媒体下载验证，B站已完成解析及下载链路验证。

| 平台 | 示例链接 | 验证结果 |
| --- | --- | --- |
| B站 | `https://www.bilibili.com/video/BV1xx411c7mD` | 成功解析格式 `30216`；下载链路已验证 |
| AcFun | `https://www.acfun.cn/v/ac35457073` | 成功下载 MP4，约 13.2 MB |
| 虎牙视频 | `https://www.huya.com/video/play/1002412640.html` | 成功下载 MP4，约 864 KB |

前端“试试”按钮已固定使用上述三个链接。

## 4. 当前限制

- 仅面向有权保存的、公开且不需要登录、Cookie、DRM 或付费授权的内容。
- 平台与具体作品是否可用取决于上游访问策略和 `yt-dlp` 当前版本。
- 当前服务器出口在测试时被抖音与 YouTube 的会话/反机器人验证拦截，因此这两个平台不作为已验证支持范围。
- 西瓜视频、小红书等部分平台在测试时需要会话或未返回可用视频格式。
- 不实现 Cookie 上传、登录态伪造、验证码绕过或 DRM 绕过。

## 5. 维护指引

1. 升级 `backend/vendor/yt-dlp` 源码前，先在测试环境验证 B站、AcFun、虎牙样本。
2. 保持 `yt-dlp` 作为独立 vendored 源码目录；不要直接修改其提取器代码。业务适配放在 `backend/app/ytdlp.py`。
3. 每次改动下载交付逻辑后，验证封面代理、带请求头的流式代理、Range 请求和中文文件名。
4. 生产环境应配置文件大小、并发量、磁盘空间与允许来源域名；对外提供服务前应补充 IP 限流与可观测性。

## 6. 验证记录

- 后端单元测试：`python -m pytest tests -q`，最近通过 27 项。
- 前端测试：`npm test -- --run`，最近通过 4 项；生产构建 `npm run build` 成功。构建仅提示 Markmap 相关 chunk 体积较大，未阻断发布。

## 7. AI 学习助手与本次增量

项目已增加独立的 `/api/v1/ai` 能力，优先基于公开视频已有字幕提供学习辅助；无字幕时可在服务器本地临时转录公开音频：

- 自动发现字幕轨道，优先中文人工字幕，并展示字幕转录内容。
- 调用 DeepSeek 生成摘要、视频大纲、核心知识点、关键词和思维导图。
- 支持基于字幕证据的问答，并返回对应时间范围引用。
- 支持字幕导出为 UTF-8 `SRT` 或 `TXT` 文件。
- 思维导图支持 Markmap 渲染、全屏查看、SVG 导出和高清 PNG 导出。
- 摘要过程支持 SSE 增量输出；AI 任务与问答仍只保存在内存中并按 TTL 清理。

## 9. 会员购买与 Stripe 支付（2026-09-20）

- 新增邮箱密码账号体系；密码使用 Argon2id，登录态使用服务端持久会话、HttpOnly Cookie 和 CSRF Token。
- SQLite 持久保存用户、会话、订单、一次性会员、订阅状态、每日配额、Webhook 事件和客服工单，并通过 Docker 独立数据卷保存。
- Stripe Checkout 支持默认一次性购买 30 天 VIP，以及用户主动勾选的月度自动续费；订阅可以通过 Stripe Customer Portal 取消。
- Webhook 使用原始请求体验签、Stripe Event ID 去重、数据库事务、金额/币种复核和订阅事件时间戳防乱序回退。
- 免费版每日 5 次且最高 720P；VIP 解锁 4K/8K（取决于来源实际格式）、无限下载、批量下载、字幕下载与 DeepSeek 翻译、AI 总结/问答和客服工单。
- 真实本地支付使用 Stripe CLI 转发 Webhook；完全离线时通过 Fake Gateway 与签名/事件测试覆盖支付业务，但不能打开真实 Stripe Checkout。

本次增量同时优化格式展示：`yt-dlp` 原始格式按分辨率去重，只向前端展示 1080P、720P、480P、360P 中实际存在的格式，并优先选择 MP4、带音频和较高码率的候选流。

本次增量验证：后端新增字幕导出、AI 端到端和格式筛选测试；前端新增字幕下载 API 测试、思维导图交互和导出入口。

## 8. 同屏工作区与元数据补全（2026-09-20）

- 下载结果页采用左侧视频信息、右侧 AI 学习工作区的 3:7 同屏布局。左栏展示封面、标题、作者、平台、播放量、简介和清晰度选择；格式卡片保持原有浅色文字样式。
- AI 侧优先从视频自带字幕生成 Markdown 视频大纲；无可用字幕轨道时自动使用本地 Whisper 音频转录。字幕、思维导图和基于字幕证据的问答功能继续可用。
- 后端将流式 Markdown 持久保留在内存任务响应的 `stream_text` 字段中，确保实时大纲与最终大纲使用同一内容。Nginx 对 SSE 关闭缓冲与缓存。
- Bilibili 的 `https://bilibili.com/video/BV1mAAmzqEfP` 已实际验证返回标题、封面、时长、作者“程序员鱼皮”、简介、平台 `Bilibili`、播放量及 1080P/720P/480P/360P 格式。
